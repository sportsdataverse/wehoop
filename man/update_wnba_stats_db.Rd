% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_wnba_stats.R
\name{update_wnba_stats_db}
\alias{update_wnba_stats_db}
\title{\strong{Build/update wehoop WNBA Stats database}}
\usage{
update_wnba_stats_db(
  dbdir = ".",
  dbname = "wehoop_db",
  datasets = NULL,
  seasons = TRUE,
  db_connection = NULL
)
}
\arguments{
\item{dbdir}{Directory in which the database is or shall be located.}

\item{dbname}{File name of an existing or desired SQLite database within
\code{dbdir}.}

\item{datasets}{Character vector of dataset shortnames (see Details).}

\item{seasons}{Optional integer vector of seasons to load. Defaults to all
available seasons (\code{seasons = TRUE}).}

\item{db_connection}{A \code{DBIConnection} object. When supplied, \code{dbdir} and
\code{dbname} are ignored.}
}
\value{
Invisibly returns \code{TRUE} on success.
}
\description{
\code{update_wnba_stats_db()} writes one or more WNBA Stats API
datasets into a database. Mirrors the WNBA / WBB \verb{update_*_db()} shape
but points at the \verb{load_wnba_stats_*} family. Each dataset is written
to its own table, named \verb{wnba_stats_<dataset>}.
}
\details{
Unlike \code{update_wnba_db()} / \code{update_wbb_db()}, there is no historical
"play-by-play table" default to preserve, so \code{datasets} is required.

Valid \code{datasets} values:
\code{"schedule"}, \code{"player_game_logs"}, \code{"pbp"}, \code{"rosters"}, \code{"coaches"},
\code{"player_stats"}, \code{"lineups"}, \code{"team_stats"}, \code{"standings"},
\code{"draft"}, \code{"shots"}, \code{"game_rosters"}, \code{"officials"}.
}
