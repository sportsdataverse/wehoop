% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wnba_tracking.R
\name{wnba_tracking_aggregate}
\alias{wnba_tracking_aggregate}
\title{\strong{Aggregate WNBA Player-Tracking Stats Across Multiple Seasons}}
\usage{
wnba_tracking_aggregate(
  seasons,
  season_types = "Regular Season",
  per_mode = "Totals",
  pt_measure_type = "Drives",
  player_or_team = "Player",
  ...
)
}
\arguments{
\item{seasons}{Numeric or character vector of WNBA season years, e.g.
\code{c(2023, 2024)}.}

\item{season_types}{Character vector of season type(s), e.g.
\code{"Regular Season"} or \code{c("Regular Season", "Playoffs")}.
Defaults to \code{"Regular Season"}.}

\item{per_mode}{Per-mode string passed to \code{leaguedashptstats}
(default \code{"Totals"}). Use \code{"Totals"} for aggregation; per-game
averages are not meaningfully additive.}

\item{pt_measure_type}{Tracking measure type string, e.g. \code{"Drives"},
\code{"SpeedDistance"}, \code{"Passing"}. Passed directly to the endpoint.
Default \code{"Drives"}.}

\item{player_or_team}{\code{"Player"} (default) or \code{"Team"}. Determines the
entity key used to group aggregation (\code{PLAYER_ID} or \code{TEAM_ID}).}

\item{...}{Additional arguments forwarded to the fetch helper
(e.g., \code{proxy}).}
}
\value{
A \code{data.frame} (tibble-compatible) with one row per entity
(\code{PLAYER_ID} or \code{TEAM_ID}).  Count columns are summed across all
requested seasons/season types; \verb{*_FG_PCT} and \verb{*_FT_PCT} are
recomputed from summed numerators/denominators; \verb{*_PCT} "\% of total"
columns are dropped; identity string columns are carried from the
first season.  Returns a 0-row frame when no data are available.
}
\description{
Fetches \code{leaguedashptstats} dashboards for each combination of \code{seasons}
and \code{season_types}, then aggregates the results by entity
(\code{PLAYER_ID} for Player, \code{TEAM_ID} for Team) following the contract
ported from \code{sportsdataverse-py}'s \code{aggregate_tracking_frames}:
\itemize{
\item \strong{Identity columns} (\verb{*_ID}, \verb{*_NAME}, \verb{*_ABBREVIATION}, and any other
non-numeric string column such as \code{TEAM_CITY}) are carried via \code{first()}
— never summed.
\item \strong{\verb{*_FG_PCT} / \verb{*_FT_PCT}} are recomputed from the summed FGM/FGA and
FTM/FTA denominators (denom 0 → \code{NA}); a \verb{*_FG_PCT} / \verb{*_FT_PCT} column
whose makes/attempts pair is absent is dropped rather than recomputed.
\item \strong{Other \verb{*_PCT} columns} (e.g., \code{DRIVE_PTS_PCT} — "\% of total" rates)
are \strong{dropped} from the output because they are not additive.
\item \strong{All remaining numeric columns} are summed (numeric-content gate,
mirroring the \code{sportsdataverse-py} \code{dtype.is_numeric()} rule).
}
}
\details{
\strong{Aggregate WNBA Player-Tracking Stats Across Multiple Seasons}

\if{html}{\out{<div class="sourceCode r">}}\preformatted{ wnba_tracking_aggregate(seasons = c(2023, 2024),
                         pt_measure_type = "Drives",
                         player_or_team  = "Player")
}\if{html}{\out{</div>}}
}
\note{
Designed and validated for COUNT-based tracking measures (e.g.
\code{"Drives"}). Rate-only columns without a \verb{_pct} suffix in some
\code{pt_measure_type} values are summed across seasons (a known limitation
shared with the \code{sportsdataverse} reference engine); verify aggregation
semantics before using rate-heavy measures.
}
\examples{
\donttest{
  try({
    df <- wnba_tracking_aggregate(
      seasons = c(2023, 2024),
      pt_measure_type = "Drives",
      player_or_team  = "Player"
    )
    print(df[1:5, c("PLAYER_ID", "PLAYER_NAME", "DRIVES", "DRIVE_FG_PCT")])
  })
}
}
\seealso{
Other WNBA Player Tracking Functions: 
\code{\link{wnba_boxscoreplayertrackv2}()}
}
\author{
Saiem Gilani
}
\concept{WNBA Player Tracking Functions}
