% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_wbb.R
\name{load_wbb_shots_manifest}
\alias{load_wbb_shots_manifest}
\alias{load_wbb_shots}
\title{\strong{Load wehoop WBB Shots}}
\usage{
load_wbb_shots_manifest()

load_wbb_shots(
  seasons = most_recent_wbb_season(),
  ...,
  dbConnection = NULL,
  tablename = NULL
)
}
\arguments{
\item{seasons}{A vector of 4-digit years associated with given women's
college basketball seasons. (Min: 2002)}

\item{...}{Additional arguments passed to an underlying function that writes
the season data into a database.}

\item{dbConnection}{A \code{DBIConnection} object, as returned by \code{\link[DBI:dbConnect]{DBI::dbConnect()}}}

\item{tablename}{The name of the shots data table within the database}
}
\value{
Returns a \code{wehoop_data} tibble with one row per shot attempt.\tabular{lll}{
   col_name \tab types \tab description \cr
   game_id \tab character \tab Unique game identifier. \cr
   season \tab integer \tab Season identifier (4-digit year or 'YYYY-YY' string). \cr
   period_number \tab integer \tab Numeric period (1-4 for quarters; 5+ for OT). \cr
   clock_display_value \tab character \tab Game clock display string (e.g. '8:32'). \cr
   team_id \tab character \tab Unique team identifier. \cr
   athlete_id_1 \tab character \tab Primary athlete identifier (e.g. shooter). \cr
   athlete_id_2 \tab character \tab Secondary athlete identifier (e.g. assister / fouler). \cr
   type_id \tab character \tab Type identifier (numeric). \cr
   type_text \tab character \tab Display text for the type field. \cr
   scoring_play \tab logical \tab TRUE if the play resulted in points scored. \cr
   score_value \tab integer \tab Point value of the play (2 / 3 / 1). \cr
   coordinate_x \tab numeric \tab X coordinate on the court (half-court layout). \cr
   coordinate_y \tab numeric \tab Y coordinate on the court (half-court layout). \cr
   coordinate_x_raw \tab numeric \tab X coordinate as returned by the API before any adjustment. \cr
   coordinate_y_raw \tab numeric \tab Y coordinate as returned by the API before any adjustment. \cr
}
}
\description{
\code{load_wbb_shots_manifest()} returns the per-season manifest
CSV (\code{season}, \code{row_count}, \code{generated_at_utc}, \code{source_endpoint}) for
the shots release tag without downloading any season's full data.

Loads shot events parsed from ESPN women's college basketball
play-by-play feeds. One row per shot attempt (made or missed), with court
coordinates and shot metadata. Backed by the \code{wehoop-wbb-data} pipeline
that reads raw JSONs from \code{wehoop-wbb-raw} and publishes parquet/rds
artifacts to the \code{espn_womens_college_basketball_shots} release tag.
}
\examples{
\donttest{
  try(load_wbb_shots(seasons = most_recent_wbb_season()))
}
}
\seealso{
Other WBB loader functions: 
\code{\link{load_wbb_game_rosters_manifest}()},
\code{\link{load_wbb_officials_manifest}()},
\code{\link{load_wbb_player_core}()},
\code{\link{load_wbb_player_stats_manifest}()},
\code{\link{load_wbb_rosters_manifest}()},
\code{\link{load_wbb_standings_manifest}()},
\code{\link{load_wbb_team_stats_manifest}()}
}
\concept{WBB loader functions}
