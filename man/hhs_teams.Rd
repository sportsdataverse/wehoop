% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hhs.R
\name{hhs_teams}
\alias{hhs_teams}
\title{\strong{Her Hoop Stats NCAA Team Single-Season Index}}
\usage{
hhs_teams(
  min_season = most_recent_wbb_season(),
  max_season = most_recent_wbb_season(),
  division = 1,
  email = NULL,
  password = NULL
)
}
\arguments{
\item{min_season, max_season}{Season range (4-digit ending year; \code{2024} =
2023-24). Default both to \code{most_recent_wbb_season()}.}

\item{division}{NCAA division: \code{1} (default), \code{2} or \code{3}.}

\item{email, password}{Optional explicit subscription credentials.}
}
\value{
A \code{wehoop_data} tibble, one row per team-season (record, scoring and
per-100-possession columns), plus a \code{team_link} column and the requested
\code{min_season} / \code{max_season} / \code{division}.
}
\description{
\strong{Get the NCAA women's team single-season summary table from
\href{https://herhoopstats.com}{Her Hoop Stats}.}

Her Hoop Stats is a \emph{subscription} service; this logs in with your own
credentials (read from the \code{email}/\code{password} arguments, the
\code{wehoop.herhoopstats_email}/\code{..._password} options, or the
\code{HERHOOPSTATS_EMAIL}/\code{HERHOOPSTATS_PW} environment variables) and returns one
row per team-season, with a \code{team_link} column for \code{\link[=hhs_team_stats]{hhs_team_stats()}} /
\code{\link[=hhs_team_roster]{hhs_team_roster()}}.
}
\note{
Requires a Her Hoop Stats subscription. The member-table layout may
change over time; selectors here are a starting point.
}
\examples{
\dontrun{
  # requires HERHOOPSTATS_EMAIL / HERHOOPSTATS_PW
  hhs_teams(min_season = 2024, max_season = 2024, division = 1)
}
}
\seealso{
Other Her Hoop Stats Functions: 
\code{\link{hhs_team_roster}()},
\code{\link{hhs_team_stats}()}
}
\concept{Her Hoop Stats Functions}
