% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_crosswalk.R
\name{load_wnba_team_crosswalk}
\alias{load_wnba_team_crosswalk}
\alias{load_wnba_schedule_crosswalk}
\alias{load_wnba_player_crosswalk}
\alias{load_wbb_team_crosswalk}
\alias{load_wbb_schedule_crosswalk}
\alias{load_wbb_player_crosswalk}
\title{\strong{Load cached WNBA team crosswalk from the data repo}}
\usage{
load_wnba_team_crosswalk(seasons = most_recent_wnba_season())

load_wnba_schedule_crosswalk(seasons = most_recent_wnba_season())

load_wnba_player_crosswalk(seasons = most_recent_wnba_season())

load_wbb_team_crosswalk(seasons = most_recent_wbb_season())

load_wbb_schedule_crosswalk(seasons = most_recent_wbb_season())

load_wbb_player_crosswalk(seasons = most_recent_wbb_season())
}
\arguments{
\item{seasons}{A vector of 4-digit years associated with given WBB seasons.
(Min: 2014)}
}
\value{
A \code{wehoop_data} tibble of cached team-crosswalk rows.

A \code{wehoop_data} tibble of cached schedule-crosswalk rows.

A \code{wehoop_data} tibble of cached player-crosswalk rows.

A \code{wehoop_data} tibble of cached WBB team-crosswalk rows.

A \code{wehoop_data} tibble of cached WBB schedule-crosswalk rows.

A \code{wehoop_data} tibble of cached WBB player-crosswalk rows.
}
\description{
Loads cached team-crosswalk snapshots that map ESPN team
identifiers to WNBA Stats API team identifiers. One row per team-season.
Backed by the \code{wnba_crosswalk} release tag in \code{sportsdataverse-data}.

Loads cached schedule-crosswalk snapshots that map ESPN game
identifiers to WNBA Stats API game identifiers. One row per game-season.
Backed by the \code{wnba_crosswalk} release tag in \code{sportsdataverse-data}.

Loads cached player-crosswalk snapshots that map ESPN athlete
identifiers to WNBA Stats API player identifiers. One row per
athlete-season. Backed by the \code{wnba_crosswalk} release tag in
\code{sportsdataverse-data}.

Loads cached team-crosswalk snapshots that map ESPN team
identifiers to Fox Sports and Bart Torvik team identifiers for Women's
College Basketball. One row per team-season. Backed by the
\code{wbb_crosswalk} release tag in \code{sportsdataverse-data}.

Loads cached schedule-crosswalk snapshots that map ESPN game
identifiers to Bart Torvik game identifiers for Women's College
Basketball. One row per game-season. Backed by the \code{wbb_crosswalk}
release tag in \code{sportsdataverse-data}.

Loads cached player-crosswalk snapshots that map ESPN athlete
identifiers to Fox Sports player identifiers for Women's College
Basketball. One row per athlete-season. Backed by the \code{wbb_crosswalk}
release tag in \code{sportsdataverse-data}.
}
\examples{
\donttest{
  try(load_wnba_team_crosswalk(seasons = most_recent_wnba_season()))
}
\donttest{
  try(load_wnba_schedule_crosswalk(seasons = most_recent_wnba_season()))
}
\donttest{
  try(load_wnba_player_crosswalk(seasons = most_recent_wnba_season()))
}
\donttest{
  try(load_wbb_team_crosswalk(seasons = most_recent_wbb_season()))
}
\donttest{
  try(load_wbb_schedule_crosswalk(seasons = most_recent_wbb_season()))
}
\donttest{
  try(load_wbb_player_crosswalk(seasons = most_recent_wbb_season()))
}
}
\seealso{
Other WNBA Crosswalk Functions:
\code{\link[=wnba_player_crosswalk]{wnba_player_crosswalk()}},
\code{\link[=wnba_schedule_crosswalk]{wnba_schedule_crosswalk()}},
\code{\link[=wnba_team_crosswalk]{wnba_team_crosswalk()}}

Other WBB Crosswalk Functions:
\code{\link[=wbb_player_crosswalk]{wbb_player_crosswalk()}},
\code{\link[=wbb_schedule_crosswalk]{wbb_schedule_crosswalk()}},
\code{\link[=wbb_team_crosswalk]{wbb_team_crosswalk()}}
}
\concept{WBB Crosswalk Functions}
\concept{WNBA Crosswalk Functions}
